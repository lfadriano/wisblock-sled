# Third-party model NOT redistributed here (see Credits in README):
# download it and drop it in vendor/ to build.
vendor/18650_V2.STL

# OpenSCAD working files
*.csg
.*.scad
_*.scad

# Compiled mesh: derivative of CC BY-SA + CC BY-NC upstreams, whose terms
# cannot both be satisfied. Build it locally instead (private use is fine).
dist/*.stl
